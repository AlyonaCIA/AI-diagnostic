name: 'AI Diagnostic - Security Scan'
description: 'Dependency vulnerability scanning'

runs:
  using: composite
  steps:
    - name: Install pip-audit
      run: uv pip install --system pip-audit
      shell: bash
    
    - name: Run security audit
      run: |
        pip-audit --desc
      shell: bash
      continue-on-error: true
